import React from 'react';
import styles from './LogoStyles';

import { View } from 'react-native';

const Logo = () => {
  return (
    <>
      <View style={styles.container}>{Logo}</View>
    </>
  );
};

export default Logo;
